name: Security Hardening (Ensure SHA-Pinned Actions)

on:
  pull_request:
    paths:
      - ".github/workflows/*.yml"
      - ".github/workflows/*.yaml"
  push:
    branches:
      - "**"
    paths:
      - ".github/workflows/*.yml"
      - ".github/workflows/*.yaml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  ensure-sha-pinned:
    uses: the-lupaxa-project/.github/.github/workflows/reusable-ensure-sha-pinned-actions.yml@master

  status-check:
    name: Check Jobs Status
    needs:
      - ensure-sha-pinned
    if: always()
    # 🔴 IMPORTANT: this is a *job-level* uses, not under steps:
    uses: the-lupaxa-project/.github/.github/workflows/reusable-check-jobs-status.yml@master
    with:
      jobs_json: ${{ toJson(needs) }}
